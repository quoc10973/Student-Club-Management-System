version: '3.8'

services:
  # SQL Server Database
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: clubmanagement-db
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Prn232Block3w@
      - MSSQL_PID=Express
    ports:
      - "1433:1433"
    volumes:
      - mssql_data:/var/opt/mssql

  # Backend API
  backend:
    build:
      context: ./BE/ClubManagementSystem
      dockerfile: Dockerfile
    container_name: clubmanagement-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Server=db;Database=ClubManagementSystem;User Id=sa;Password=Prn232Block3w@;TrustServerCertificate=True;
      - JwtConfig__Issuer=StudenClubManagementSystem
      - JwtConfig__Audience=StudenClubManagementUser
      - JwtConfig__Key=W$7gN!f82Qz@Rk3L^p0Xv91#HcT*Mb46sYjUeDZwAqF5rP8hC
      - JwtConfig__ExpireMinutes=30
    ports:
      - "8080:8080"
      - "8081:8081"
    depends_on:
      - db
    restart: unless-stopped

volumes:
  mssql_data:
